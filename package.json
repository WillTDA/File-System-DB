{
  "name": "fsdb",
  "version": "0.0.0",
  "scripts": {
    "publish": "npm run format && npm publish -w lib",
    "pretest": "npm link lib tests",
    "test": "npm test -w tests",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^18.15.11",
    "prettier": "^2.8.7",
    "typescript": "^5.0.4"
  },
  "private": true,
  "workspaces": {
    "packages": [
      "lib",
      "tests"
    ]
  }
}
